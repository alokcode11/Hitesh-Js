// while

let index = 0 // Inialization
while (index < 10) { // Condition check 
    console.log(`value of index is ${index}`);
    index = index + 2; //updation 
}

let arr = ['a', 'b', 'c', 'd']
let i = 0
while (i < arr.length) {
    console.log(`value is ${arr[i]}`);
    i++
}

// do while loop 
// atleast ek baar to execute hoga 

let score = 11
do {
    console.log(`score is ${score}`);
    score++
} while(score < 10) // 11 print 